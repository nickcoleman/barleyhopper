import React from 'react'

import {

} from '../actions'

const INITIAL_STATE = {

}

export default (state = INITIAL_STATE, action) => {
  // console.log(action.payload)
  switch (action.type) {

    default:
      return state
  }
}
